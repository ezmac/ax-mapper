# Enable file-based API mode (instead of browser localStorage)
VITE_API_MODE=true

# Directory where project files are stored (server-side)
DATA_DIR=./data

# Port the Express API server listens on
SERVER_PORT=3001
